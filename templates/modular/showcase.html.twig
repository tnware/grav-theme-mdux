  {% set showcase_image = page.media.images|first %}

  {% if showcase_image %}
  <div class="modular-row showcase header" style="background-image: url({{ showcase_image.url }});">
    <div class="container">
  {% else %}
  <div class="modular-row showcase header">
    <div class="container">
  {% endif %}
        {{ content }}
      </div>
    </div>
