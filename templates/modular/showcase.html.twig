  {% set showcase_image = page.media.images|first %}

  {% if showcase_image %}
  <div class="row showcase header" style="background-image: url({{ showcase_image.url }});">
    <div class="col">
  {% else %}
  <div class="row showcase header">
    <div class="col">
  {% endif %}
        {{ content }}
      </div>
    </div>
